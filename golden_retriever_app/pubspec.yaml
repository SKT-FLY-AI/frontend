# pubspec.yaml

name: golden_retriever_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0' # Dart SDK 버전 범위를 최신 상태로 유지

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  image_picker: ^1.0.0
  cupertino_icons: ^1.0.6
  # 필요한 추가 패키지를 여기에 포함합니다.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # 최신 버전 확인

flutter:
  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  # 앱에서 사용하는 에셋이 있다면 여기에 추가합니다.
